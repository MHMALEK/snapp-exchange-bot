"""
English UI copy (optional).

1. Fill `STRINGS` with the same keys as `fa.py`.
2. In `exchange_money_bot/i18n/__init__.py`, import this module and add `"en": en.STRINGS` to `_TABLES`.
3. Set `DEFAULT_LOCALE = "en"` in `constants.py` or extend `t()` to pick locale per user later.
"""

STRINGS: dict[str, str] = {}
